import {StyleSheet} from 'react-native';
import Colors from './Theme';

// defined the common styles
export default StyleSheet.create({
  containerTop: {
    flex: 1,
    backgroundColor: Colors.white,
  },
  flex: {flex: 1},
  row: {flexDirection: 'row'}
});
